// Supabase client removed — application now serves images from the public folder.
// If you later need Supabase again, recreate a client here and load credentials
// from environment variables instead of hardcoding them.

export const supabase = null;